import BuingProcessForm from "../features/buy-process/BuingProcessForm";
import { Box } from "@mui/material";

const BuingProcess = () => {

    return (
        <Box sx={{ display: 'flex', justifyContent: 'center', width: '100%' }}>
            <BuingProcessForm />
        </Box>
    )
}

export default BuingProcess