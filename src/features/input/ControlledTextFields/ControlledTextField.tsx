
type Props = {
    isError?:(value:string)=>{error:boolean,message?:string},
    isWarnig?:(value:string)=>{error:boolean,message?:string},
}

const ControlledTextField = ({isError,isWarnig}:Props) => {

}

export default ControlledTextField